# DreamFactory v2.1 Backend

This is the Python backend for DreamFactory v2.1, powered by Google ADK and Gemini 3.0.

## Setup

1.  Install uv: `curl -LsSf https://astral.sh/uv/install.sh | sh`
2.  Run server: `uv run uvicorn main:app --reload`
